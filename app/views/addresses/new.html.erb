<% @page_title = 'Neue Address anlegen' %>

<%= error_messages_for :address %>
<% form_for :customer, @address, :url => addresses_url, :html => {:method => :post} do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag 'Speichern' %>
<% end %>